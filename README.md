# serichai-web-portal
Web Portal for internaly core functionality of Serichai and Ch.Paisarn

## Windows install (no Docker)

Scripts in [`scripts/windows/`](scripts/windows/README.md) install the dependencies, register the portal to start at boot, open the firewall for the local network, and optionally set a static IP.
